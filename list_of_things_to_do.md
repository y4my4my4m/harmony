1. the sidebar profile view goes out of bound if the screen is too small
2. the profile view on clicking a username might be out of bound (if too close to top or bottom of the screen)

i want to create an auto-suggest system

if we do `:` and another character after that, it should show a popup above where we're writing (similar to the emoji popup but properly aligned near the text) so that we can auto select an emoji, pretty much exactly like discord does it.

i also want auto suggest users AND a mentions system like `@bob` if we type `@b` it'll also show other users in the server that has a name that starts with that.

Make sure the code is 
1. reusable
2. clean
3. scalable
4. DRY
5. professional

AND if you ever need to use supabase, be sure to use the node package that's already in place


---

Please create a professional, scalable and DRY server settings ressembling what discord is doing.
Make sure the code is clean.

You may refactor and change almost everything but dont lose functionality